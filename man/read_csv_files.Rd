% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_files.R
\name{read_csv_files}
\alias{read_csv_files}
\title{Read raw data from multiple files}
\usage{
read_csv_files(path, names = NULL, types = NULL)
}
\arguments{
\item{path}{A string pointing to the directory pointing to the relevant files.}

\item{names}{A character vector containing the column names.}

\item{types}{A column specification created with \code{\link[readr]{cols}}.}
}
\description{
Reads all the files contained in the directory pointed to by \code{path} into a
single data frame.
}
\examples{
# Create a temporary directory for the example
tempdir <- tempdir()
tempdir <- file.path(tempdir, "example")
dir.create(tempdir)

# Generate a sample data frame
df <- data.frame(
x = c("a", "b", "c"),
y = c(1, 2, 3)
)

# Define the file path
file_path <- file.path(tempdir, "example.csv")

# Save the data frame as a CSV file
write.csv(df, file = file_path, row.names = FALSE)

# Define column names and types
names <- c("x", "y")
types <- readr::cols(
"x" = readr::col_character(),
"y" = readr::col_integer()
)

# Read the data using read_centrix()
read_csv_files(tempdir, names, types)

# Delete the temporary directory
unlink(tempdir, recursive = TRUE)

}
